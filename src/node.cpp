/**
 * @file node.cpp
 * @brief Node3D is header-only; kept as a translation unit for a stable place to add future out-of-line members.
 */
#include "risersim/node.hpp"

namespace risersim {
// Node3D member implementations if needed
}
